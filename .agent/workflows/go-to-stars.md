---
description: "Go to Stars" Protocol: Initialization and Readiness Check
---

# Go to Stars Protocol 🚀

Этот воркфлоу запускает проверку готовности проекта перед началом активной разработки.

## 1. Инициализация (Environment & Tools)

Проверяем наличие необходимых глобальных инструментов.

```bash
node -v
npm -v
# Проверка наличия GitHub CLI
gh --version
# Проверка наличия Vercel CLI
vercel --version
```

## 2. Загрузка Правил (Context)

> [!NOTE]
> В режиме агента правила загружаются из `CLAUDE.md` и `.agent/skills`.
> Убедитесь, что `CLAUDE.md` содержит актуальные инструкции по Next.js 15 и Mobile-First.

## 3. Автоматизированный Чек-ап (System Check)

Запускаем скрипт диагностики проекта.

```bash
# Убедимся, что ts-node установлен (для запуска скрипта)
# Если нет, запускаем через bun или tsx, или компилируем на лету.
# Для простоты используем npx tsx
npx tsx scripts/go-to-stars.ts
```

// turbo
```bash
npx tsx scripts/go-to-stars.ts
```

Если скрипт вернул ошибку, исправьте проблемы перед продолжением.

## 4. Готовность

Если чек-ап прошел успешно:
1.  Откройте `task.md` и актуализируйте задачи.
2.  Запустите сервер: `npm run dev`.
